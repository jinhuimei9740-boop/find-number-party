# 找数字聚会

一个 1-4 人线上找数字小游戏。创建房间后复制邀请链接给朋友，设置数字区间和图纸数量，开始后所有玩家抢先点击当前目标数字计分。

## 本地运行

```bash
node server.js
```

打开 `http://localhost:4173`。

## Render 部署

- Build Command: 留空，或填 `npm install`
- Start Command: `npm start`
- Node 服务会自动读取 Render 提供的 `PORT` 环境变量

部署成功后，打开 Render 提供的网址创建房间并分享链接。
